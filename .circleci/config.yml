version: 2
jobs:
    build:
        branches:
            only:
                - master
                - develop
        working_directory: ~/repo
        docker:
            - image: circleci/node:14
        steps:
            - checkout
            - run:
                name: Install Node dependencies
                command: npm install
            - run:
                name: Unit tests
                command: npm run test
